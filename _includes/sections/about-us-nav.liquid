<nav class="about-us-nav">
  {% for link in site.data.quick-links-about-us %}
    <li>
      <a class="about-us-nav__item {% if page.url == link.link_url %}about-us-nav__item--active{% endif %}" href="{{link.link_url}}">{{link.title}}</a>
    </li>
  {% endfor %}
</nav>
