{% comment %}
This is a video component/utility. Things to be passed:
@param file - the video, path is relative to assets/videos
@param caption - optional
@param type - "left", "right", "full"
{% endcomment %}
{% capture full_video_path %}{{site.baseurl}}/assets/videos/{{include.file | url_escape}}{% endcapture %}
<figure
  class='video
    {% if include.type == "left" %}
        video--left
    {% elsif include.type == "right" %}
        video--right
    {% elsif include.type == "full" %}
        video--full
    {% else %}
        video-full
    {% endif %}'
>
  <video controls>
    <source src="{{full_video_path}}">
  </video>

  {% if include.caption %}
  <figcaption class="video__caption">{{include.caption}}</figcaption>
  {% endif %}
</figure>
